import dynamic from 'next/dynamic';
import type { Metadata } from 'next';
import './globals.css';
import Providers from './providers';
import Container from '../components/ui/Container';
import PinGuard from '../components/security/PinGuard';
import { APP } from '../lib/app.config';

const /components/_

export const metadata: Metadata = { title: APP.NAME, description: 'Mini App' };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ru" className="light" style={{ colorScheme:'light' }} suppressHydrationWarning>
      <body suppressHydrationWarning>
        <Providers>

        <Providers>
          <div style={{ paddingBottom: APP.TABBAR_HEIGHT }}>
            <Container>{children}</Container>
          </div>
          <PinGuard />
          </>
        </Providers>
                </>
        </Providers>
      </body>
    </html>
  );
}
